// Filled by the Vercel build from environment variables. Keep empty locally;
// the admin settings form can still be used for a local draft.
window.__SUPABASE_CONFIG__ = {
  url: '',
  publishableKey: ''
};
